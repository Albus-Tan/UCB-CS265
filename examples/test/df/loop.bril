@main {
.entry:
  y: int = const 3;
  a: int = const 4;
  x: int = const 0;
  z: int = id y;
  jmp .header;
.header:
  c: bool = lt x a;
  br c .loop .exit;
.loop:
  w: int = const 1;
  x: int = add x w;
  y: int = add y w;
  jmp .header;
.exit:
  print x;
}
